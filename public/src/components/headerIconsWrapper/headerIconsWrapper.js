import "./headerIconsWrapper.css";
import { createElem } from "../../functions/createElem";

const headerIconsWrapperHTML = `
`;

export function headerIconsWrapper() {
  createElem(
    [
      "div",
      "headerIconsWrapper",
      "headerIconsWrapper",
      "header"
    ],
    headerIconsWrapperHTML
  );
}
headerIconsWrapper();
